# 知识库文件模板

本文件包含 coding-knowledge 所有输出文件的标准模板。生成文件时必须参考此模板。

---

## 通用文件格式

每个知识文件遵循统一结构：

```markdown
# {标题}

> 知识层级: {infra|business|repo}
> 信息来源: {code-scan|user-input|inferred}
> 置信度: {high|medium|low}
> 最后更新: {timestamp}

## 概要
{2-3 句话概括核心内容}

## 内容
{主体内容}

## 信息来源明细
- {具体的代码文件路径或用户输入记录}
```

**置信度标注**：
- `high`: 直接从代码或配置中提取，或用户明确确认
- `medium`: 从代码模式推断，有合理依据但未经确认
- `low`: 基于常见实践的推测，建议用户验证

---

## repos/ 层模板

### .scan-snapshot.md

```markdown
# Scan Snapshot — {仓库名}
<!-- 基于 scan-result.json 自动合成 -->

## 技术栈
- 框架: [如 Spring Boot 2.1.3]
- ORM: [如 MyBatis-Plus 3.0.6]
- 数据库: [如 MySQL]
- 构建: [如 Maven 多模块]

## 模块结构
（从 directory_tree 提取，标注每层文件数和职责）

## 数据模型
| Entity/Model | 表名 | 关键字段（名:类型） | 说明 |
|-------------|------|-------------------|------|

## API 路由
| 方法 | 路径 | Handler 类:方法 | 行号 | 说明 |
|------|------|----------------|------|------|

## Service 核心方法
| Service 类 | 方法名 | 逻辑摘要 | 调用的 DAO/外部服务 |
|-----------|--------|---------|---------------------|

## 枚举与常量
| 枚举类 | 文件路径 | 值列表 |
|--------|---------|--------|

## 配置项
| 配置 Key | 所在文件 | 默认值 | 说明 |
|---------|---------|--------|------|

## 跨服务调用
| 目标服务/appid | 调用方式 | 接口 | 代码位置 |
|---------------|---------|------|---------|

## MQ 消息
| Topic/Queue | 方向(发布/消费) | 触发/处理逻辑 | 代码位置 |
|-------------|----------------|--------------|---------|

## 数据库 DDL
（如找到 schema.sql 或 migration，记录 DDL 来源路径和关键表结构）

## 定时任务
| 任务名 | 调度方式 | Cron/频率 | 逻辑摘要 | 代码位置 |
|--------|---------|----------|---------|---------|
```

### architecture.md

```markdown
# {仓库名} 代码架构

> 知识层级: repo | 信息来源: code-scan | 置信度: high | 最后更新: {date}

## 概要
{2-3 句话}

## 项目概要
- GroupId / ArtifactId / 包名 / 启动类
- 技术栈版本

## 模块结构
（目录树 + 每层职责）

## 功能模块
（按 Controller/Service/DAO 分层列出核心类和职责）

## 职责边界
### 本服务负责
- {列出核心职责，如"合同生成、签章流程管理、合同状态机"}

### 本服务不负责
- {明确排除项，如"不负责用户鉴权（由网关处理）"、"不负责文件存储（调用 OSS 服务）"}

### 常见误解
- {如"虽然有 PaymentController，但只是代收合同签章费用的入口，真正的支付逻辑在 payment_srv"}
- {如"order_srv 和 fulfill_srv 名称相似，但前者负责订单处理，后者负责履约"}

## 关键依赖
（列出核心中间件和第三方 SDK）
```

### codebase-index.md

```markdown
# {仓库名} 代码索引

按功能模块组织的文件清单，标注每个文件的职责。

## {模块名}
| 文件 | 路径 | 职责 |
|------|------|------|
```

### symbols.md

```markdown
# {仓库名} 代码符号索引

## 对外 API
| 类/接口 | 方法签名 | 路径 | 说明 |
|---------|----------|------|------|
| TradeController | createOrder(CreateOrderRequest): Response<OrderDTO> | src/.../TradeController.java:45 | 创建订单 |

## 核心领域模型
| 类名 | 路径 | 关键字段 | 说明 |
|------|------|----------|------|

## 核心业务服务
| 类/接口 | 方法签名 | 路径 | 说明 |
|---------|----------|------|------|

## 数据访问层
| 类/接口 | 方法签名 | 路径 | 说明 |
|---------|----------|------|------|
```

**symbols.md 质量要求**：
- **禁止占位描述**：不允许 "(多个方法)"、"(签章方法)" 等模糊占位符。每个方法必须写出完整签名。
- **必须从源码提取**：使用 Grep 搜索注解定位核心类，然后 Read 类文件提取 public 方法签名。
- **按 Controller 分组**：对外 API 按 Controller 类分组，每个 Controller 列出所有 public 接口方法。
- **包含行号**：方法路径必须包含行号（如 `:45`）。
- **聚焦核心**：每个仓库聚焦 Controller/Service/DAO 三层，总条目 50-100 个核心符号。

### database-schema.md

```markdown
# {仓库名} 数据库结构

> 信息来源: {DDL文件路径 | ORM代码推断}

## 数据源
| 数据源名 | 数据库类型 | 用途 |
|---------|-----------|------|

## 核心表结构
### {表名}
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|

## 状态枚举
| 枚举类 | 对应字段 | 值 → 含义 | 状态流转 |
|--------|---------|----------|---------|

## 索引
| 表名 | 索引名 | 字段 | 类型 |
|------|--------|------|------|
```

### call-chains.md

```markdown
# {仓库名} 核心业务调用链

## {流程名，如"合同生成"}
Controller.method()
  → Service.method()
    → DAO.method() / 外部服务调用
    → MQ.publish(topic)

关键逻辑：
1. ...
2. ...

## 跨服务调用
| 目标服务 | 调用方式 | 接口 | 用途 |
|---------|---------|------|------|

## MQ 消息
| Topic | 方向 | 消息体 | 触发/处理 |
|-------|------|--------|----------|

## 定时任务
| 任务名 | Cron | 逻辑摘要 |
|--------|------|---------|
```

---

## business/ 层模板

### glossary.md

```markdown
# 业务术语表

> 信息来源: code-scan + user-input | 最后更新: {date}

## 使用说明
AI 在生成需求文档或编写代码时，必须使用本术语表中的标准术语。遇到不在表中的新术语，应询问用户确认后补充。

## 核心术语

| 术语 | 定义 | 同义词/各系统别名 | 易混淆术语 |
|------|------|------------------|-----------|
| 合同 | 双方签署的具有法律效力的电子文件 | contract（代码中）、协议（业务口语） | 订单（order）：订单是交易记录，合同是法律文件 |
| 签章 | 对电子合同进行数字签名和加盖电子印章的过程 | seal/sign（代码中）、电签（业务口语） | 审批（approve）：审批是内部流程，签章是法律行为 |

## 跨系统术语映射

当同一概念在不同系统/团队中使用不同名称时，记录映射关系：

| 标准术语 | 系统 A 中的叫法 | 系统 B 中的叫法 | 说明 |
|---------|----------------|----------------|------|

## 状态枚举术语

从代码枚举中提取的状态值及其业务含义（避免 AI 编码时使用错误状态值）：

| 枚举类 | 值 | 业务含义 | 使用场景 |
|--------|-----|---------|---------|
```

### overall-architecture.md

（无特定模板，按实际业务架构自由组织）

### domains/{domain}/overview.md

```markdown
# {域名} 领域概述

> 知识层级: business | 信息来源: code-scan + user-input | 置信度: {level} | 最后更新: {date}

## 概要
{2-3 句话概括此域的核心业务}

## 核心业务流程
### {流程名，如"合同生成流程"}
1. {步骤1} → 涉及仓库: {repo_name}
2. {步骤2} → 涉及仓库: {repo_name}
3. ...

## 关键业务规则
| 规则 | 描述 | 涉及仓库 | 代码位置 |
|------|------|---------|---------|

## 域内仓库职责分工
| 仓库 | 在此域中的角色 | 核心能力 |
|------|---------------|---------|

## 领域术语（详见 glossary.md）
本域涉及的核心术语：{列出 3-5 个，链接到 glossary.md}
```

### domains/{domain}/domain-model.md

```markdown
# {域名} 领域模型

> 知识层级: business | 信息来源: code-scan | 最后更新: {date}

## 核心实体关系

| 实体 | 所在仓库 | 对应表 | 核心字段 | 与其他实体的关系 |
|------|---------|--------|---------|----------------|

## 状态机
### {实体名} 状态流转

| 当前状态 | 可转向 | 触发动作 | 触发代码位置 |
|---------|--------|---------|------------|

## 数据流向
{标注核心数据在各仓库间的流转路径}
{仓库A} 写入 → {表/MQ} → {仓库B} 读取
```

### domains/{domain}/cross-service.md

```markdown
# {域名} 跨仓库调用关系

> 信息来源: code-scan（从各仓库扫描快照自动提取）

## 调用拓扑
{仓库A} ──[RPC/REST]-→ {仓库B}
{仓库A} ──[MQ: topic]-→ {仓库C}

## 调用明细
| 调用方 | 被调方 | 方式 | 接口/Topic | 用途 |
|--------|--------|------|-----------|------|

## 共享数据
| 数据库/表 | 读取方 | 写入方 | 说明 |
|----------|--------|--------|------|
```

---

## infra/ 层模板

### tech-stack.md

```markdown
# 技术栈规范

> 知识层级: infra | 信息来源: code-scan + user-input | 最后更新: {date}

## 概要
{2-3 句话概括技术栈选型}

## 语言与框架
| 技术 | 版本 | 用途 | 使用的仓库 |
|------|------|------|-----------|

## ORM 与数据访问
| 技术 | 版本 | 配置要点 |
|------|------|---------|

## 构建工具
| 工具 | 配置要点 |
|------|---------|

## 编码时注意
- {从代码中提取的实际约定}
```

### middleware.md

```markdown
# 基础设施与中间件规范

> 知识层级: infra | 信息来源: code-scan + user-input | 最后更新: {date}

## 概要
{2-3 句话}

## RPC / 服务间调用
| 方式 | 使用场景 | 配置要点 | 示例仓库 |
|------|---------|---------|---------|

## 消息队列
| MQ 类型 | 使用场景 | 命名规范 | 注意事项 |
|---------|---------|---------|---------|

## 缓存
| 类型 | 使用场景 | Key 命名规范 | 过期策略 |
|------|---------|-------------|---------|

## 数据库
| 类型 | 使用规范 |
|------|---------|

## 日志与监控
| 组件 | 用途 | 接入方式 |
|------|------|---------|

## 编码时注意
- {如"MQ 消费必须实现幂等，使用 messageId 去重"}
```

### app-architecture.md

```markdown
# 应用选型与分层规范

> 知识层级: infra | 信息来源: code-scan + user-input | 最后更新: {date}

## 代码分层
| 层级 | 命名规范 | 职责 | 示例 |
|------|---------|------|------|
| Controller | *Controller.java | 入参校验、调用 Service、返回 DTO | TradeController |
| Service | *Service.java / *ServiceImpl.java | 业务逻辑、事务管理 | TradeServiceImpl |
| DAO/Mapper | *Mapper.java / *Repository.java | 数据访问 | TradeMapper |
| Entity/DO | *Entity.java / *DO.java | 数据库映射 | TradeEntity |
| DTO/VO | *DTO.java / *VO.java | 数据传输对象 | TradeDTO |

## 异常处理
| 异常类型 | 处理方式 | 示例 |
|---------|---------|------|

## 编码时注意
- {如"Controller 层禁止写业务逻辑"}
```

### code-quality.md

```markdown
# 代码质量规范

> 知识层级: infra | 信息来源: user-input | 置信度: {取决于用户是否回答} | 最后更新: {date}

## 代码审查
- {如"所有代码必须经过 code review"}

## 测试要求
- {如"新增接口必须有单元测试"}

## 静态检查
| 工具 | 配置 | 强制级别 |
|------|------|---------|

## 编码时注意
- {从代码中提取的实际约定}
```

### cicd.md / security.md

主要从 Phase 2 用户回答填充，无回答则不生成。格式参考通用文件格式。

---

## INDEX.md 模板

### repos/INDEX.md（必须包含场景路由表）

```markdown
# 代码仓库层索引

> 业务: {业务名称} | 包含仓库: {数量}

## 仓库清单
| 仓库 | 类型 | 所属域 | 一句话职责 |
|------|------|-------|-----------|

## 场景路由表

当 AI 需要修改某个功能时，按此表定位到具体仓库和模块：

| 要改什么 | 找哪个仓库 | 看哪个模块/文件 | 参考文档 |
|---------|-----------|----------------|---------|

## 同名/易混淆模块区分

当多个仓库存在功能相似的模块时，明确区分规则：

| 功能描述 | 可能误找 | 应该找 | 区分规则 |
|---------|---------|--------|---------|
```

### 通用 INDEX.md

```markdown
# {层级名称} 知识索引

> 业务: {业务名称} | 更新时间: {timestamp} | 包含仓库: {数量}

## 概览
{3-5 句话}

## 导航
| 文件 | 内容摘要 | 使用场景 |
|------|----------|----------|

## 关键信息速览
{最重要的 5-10 条信息}
```

### 顶层 INDEX.md

```markdown
# AI Coding 专家知识库

> 业务: {业务名称}
> 生成时间: {timestamp}
> 知识库版本: 1.0

## 三层知识架构

### 1. 基础技术层 → `infra/INDEX.md`
公司级技术规范和基础设施知识。编码时遇到技术选型、中间件使用、代码规范等问题时查阅。

### 2. 业务层 → `business/INDEX.md`
业务架构和领域知识。理解需求背景、业务流程、领域模型时查阅。

### 3. 代码仓库层 → `repos/INDEX.md`
各仓库的代码架构和符号索引。定位代码、理解模块职责、查找接口时查阅。

## 意图路由表

> 按你的具体意图直达所需文件，避免读无关内容。先匹配意图，只读对应列的文件。

| 你要做什么 | 第一步读 | 第二步读 | 第三步（按需） |
|-----------|---------|---------|--------------|
| **写 PRD / 需求分析** | `business/prd-reference/existing-features.md` | `business/prd-reference/business-flows.md` | `business/prd-reference/glossary.md` |
| **理解业务全局** | `business/overall-architecture.md` | `business/glossary.md` | 对应域的 `overview.md` |
| **改某个服务的代码** | `repos/INDEX.md` 的场景路由表 | 目标仓库的 `symbols.md` | 目标仓库的 `call-chains.md` |
| **改数据库表结构** | `repos/{repo}/database-schema.md` | `business/glossary.md`（确认字段含义） | — |
| **理解跨服务调用** | `business/domains/{域}/cross-service.md` | 上下游仓库的 `call-chains.md` | — |
| **技术选型 / 规范** | `infra/INDEX.md` | 对应规范文件 | — |
| **定位某个类/方法** | `repos/INDEX.md` 的场景路由表 | 目标仓库的 `symbols.md` | — |

生成时根据实际仓库和业务域补充更具体的意图行（如"改签章逻辑"→ 具体仓库路径），使路由表对该项目的 AI 消费者最大化有用。

## 路由表匹配不上时

路由表覆盖高频场景，但你的意图可能不在表里。按以下方式兜底：

1. **关键词定位**：从你的任务中提取关键词（服务名、功能名、技术组件名），到 `repos/INDEX.md` 的场景路由表中搜索
2. **业务域缩小范围**：确定任务涉及哪个业务域，读该域的 `overview.md` 了解域内仓库分工
3. **层级导航**：仍然不确定时，从上方"三层知识架构"的子 INDEX.md 开始逐层浏览

## 需要广泛理解时

以下场景不应只读 2-3 个文件，需要跨多个文件建立全局认知后再行动：

| 场景 | 建议阅读范围 |
|------|------------|
| **评估技术可行性** | 涉及仓库的 `architecture.md` + `call-chains.md` + `database-schema.md`，再加 `business/domains/{域}/cross-service.md` 评估跨服务影响 |
| **评估改动影响范围** | `business/overall-architecture.md`（全局拓扑）+ 所有可能涉及仓库的 `call-chains.md`（追踪上下游） |
| **排查跨服务问题** | 从入口仓库的 `call-chains.md` 出发，沿调用链逐个读下游仓库的 `call-chains.md` + `symbols.md` |
| **新人理解整个系统** | `business/overall-architecture.md` → `business/glossary.md` → 每个域的 `overview.md` → 核心仓库的 `architecture.md` |

生成时根据实际业务域和仓库拓扑填充具体路径。

## 使用原则

1. **先判断任务类型** — 明确的修改任务用路由表直达；探索性任务（可行性分析、影响评估、问题排查）参考上方"需要广泛理解时"
2. **INDEX 先行** — 不确定找哪个仓库时，先读 `repos/INDEX.md` 的场景路由表和易混淆模块区分
3. **symbols.md 是精确入口** — 需要定位具体代码时，直接查 symbols.md 的方法签名和行号
4. **行号仅供参考** — 知识库基于扫描生成，代码变更后行号可能偏移，以实际代码为准

## 仓库快速导航

| 仓库 | 类型 | 所属业务域 | 入口文件 |
|------|------|-----------|---------|

## 知识完整性

当前整体完整度: **{百分比}%**。详见 `knowledge-gaps.md`。
```

### knowledge-gaps.md

```markdown
# 知识完整性检查报告

> 生成时间: {timestamp}
> 整体完整度: {百分比}

## 各层完整度

| 层级 | 完整度 | 关键缺失 |
|------|--------|----------|

## 仓库扫描完整度

| 仓库 | snapshot | arch | index | symbols | db-schema | chains | 完整度 |
|------|----------|------|-------|---------|-----------|--------|--------|

## 待补充项

### [P0] 阻塞性缺失
{影响编码决策的关键信息}

### [P1] 建议补充
{有助于提升编码质量但非必须}

### [P2] 锦上添花
{了解后会更好但不影响日常编码}

## 下一步建议
{具体的补充行动建议}
```
